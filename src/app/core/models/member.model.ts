export interface AssociationMember {
  id: number;
  name: string;
  role: string;
  image?: string;
}

export interface Achievement {
  id: number;
  title: string;
  year: string;
  description: string;
}
